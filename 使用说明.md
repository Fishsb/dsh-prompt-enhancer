# 「提示词优化」插件 · 使用说明

> 对应方案 `模型与插件管理整合方案.md` 的交付物。插件为 DSH 动态 Cordis 插件（`enh-1`，当前包 `pkg-29` v18），host + client 双半部。

## 入口位置

- **优化按钮**：输入区右下角工具行、模型选择左侧（slot `conversation.input.right`）——**默认仅一个 ✨ 图标**
- **错误提示条**：输入区上方 dock（slot `conversation.input.dock`，与 todo / goal / queue 并存）——**仅在优化失败时出现**

## 按钮三态（状态直接显示在按钮上）

| 状态 | 按钮显示 | 点击行为 |
|---|---|---|
| 默认 | `✨` 图标 | 开始优化（守卫不通过时置灰，title 说明原因） |
| 优化中 | spinner + 「优化中」 | 取消优化并恢复原文 |
| 已优化 | `✓ 已优化，可撤回` | 撤回，恢复原文 |

> v8 起「已优化，可撤回」提示直接显示在按钮上，dock 不再出现优化中/已优化状态条（仅错误提示保留在 dock）。

## 操作流程

### 1. 一键优化
1. 输入任意非空、非斜杠命令的文本
2. 点击 **✨** 图标（或聚焦后按 Enter）
3. 等待独立 LLM 调用完成（按钮显示「优化中」+ loading 转圈）
4. 完成后输入框内容**被直接替换**为优化版，按钮变 **✓ 已优化，可撤回**

### 2. 撤回
- 点击按钮（此时为「✓ 已优化，可撤回」）→ 输入框恢复原文，按钮回到 ✨ 图标

### 3. 取消（优化中）
- 优化进行中点击按钮（「优化中」态）→ 请求被 abort，输入框立即恢复原文，无错误提示

### 4. 修改即失效
- 优化完成后手动修改输入框任何字符 → 按钮自动回到 ✨ 图标（撤回入口消失，不覆盖你的编辑）
- 优化进行中编辑输入框 → 结果返回时被丢弃（不覆盖编辑）

### 5. 错误提示
- 优化失败/超时 → 输入框自动恢复原文，dock 显示「优化失败：<可读原因>」+「知道了」按钮

## 语言跟随

- 插件文案（按钮/提示/错误）**跟随 DeepSeek Harness 的界面语言设置**（设置 → 语言），当前支持中文 / English
- 切换语言后无需刷新，按钮文案即时更新

## 守卫行为

| 场景 | 行为 |
|---|---|
| 空输入 / 仅空白 | 图标置灰（title 提示「请输入内容后再优化」） |
| 斜杠命令 `/cmd`（无正文） | 图标置灰（title 提示「命令内容为空」） |
| 斜杠命令 `/cmd 内容`（调用能力） | **可优化：保留 `/cmd` 前缀，仅优化其后正文**（发送时仍是同一命令） |
| @引用/能力占位后继续输入 | 可优化；优化完成后占位随新草稿自然解除 |
| 输入态 submitting/adjudicating（提交/裁定中） | 图标置灰（title「当前输入状态不允许优化」） |
| 无会话/hero 态 | 入口自动不渲染 |
| 附件 | 优化只替换文本，附件（图片等）保留（imageIds 独立存储） |

## 模型与兜底链

- 复用 DSH 已配置 provider（`settings.yaml` → `llm-pi-ai.providers.*`），不引入代理通道、不重复配置密钥
- **主模型 = 用户在「模型与插件（模型配置）」页选择的 provider/model**（默认 `opencode-go / deepseek-v4-flash`）；每次优化请求都会携带当前配置
- **兜底链可在「模型与插件（模型配置）」页配置**（默认内置，见下）：
  1. `opencode-go / deepseek-v4-flash`
  2. `opencode-go / minimax-m3`
  3. `opencode-go / kimi-k3`
- 单次请求默认超时 30s、输出上限 2000 token / 8000 字符（**均可在「优化参数」页调整**）；全部失败 → 自动恢复原文 + 可读错误提示

## 模型与插件（设置 → 模型与插件）

> v16 起将「插件管理」与「优化配置」合并为单一入口；页内三区 tab：**模型配置 / 优化参数 / 插件管理**。

### 模型配置 tab

- **主模型**（折叠区块）：provider + 模型下拉（联动 Harness 已配置 provider 的模型目录）；**思考能力开关 + 思考等级下拉**（有 reasoning 元数据的模型显示，等级显示模型原生名称，如 Off / High / Max）；「**测试连通性**」按钮显示 `✓ 可用 · 延迟ms`（或 ✗+原因）
- **兜底链**（折叠区块）：主模型失败后按此顺序尝试；每条可增/删/上移/下移，并**独立设置思考开关与等级**；「恢复默认」重置为内置 3 链
- **自定义模型**（折叠区块）：添加目录外的模型 ID（仅限已有 provider 路由），**添加即自动连通性测试**，可删除
- **加载顺序**（折叠区块）：全量模型（目录 + 自定义）上移/下移，仅影响下拉与候选的**展示顺序**

### 优化参数 tab

| 配置项 | 控件 | 档位 | 默认 |
|---|---|---|---|
| 超时时间 | 下拉 | 10s / 30s / 60s / 120s | 30s |
| 输出 Token 上限 | 下拉 | 500 / 1000 / 2000 / 4000 | 2000 |
| 输出字符上限 | 下拉 | 2000 / 4000 / 8000 / 16000 | 8000 |
| 模板 | 下拉 | 内置模板 / 自定义模板 | 内置模板 |
| 自定义模板内容 | 文本域（custom 时启用，≤4000 字符） | — | 内置模板 v4 全文 |

- **改动即保存**：任何控件变更立即写入本地存储，无保存按钮
- 配置保存在**本地浏览器 localStorage**（key `dsh.enhance.config.v2`，v1 自动迁移写回并清理），仅对当前浏览器实例生效；换浏览器/机器不共享（本地应用语义，可接受）
- 主题不在本页提供：请使用 Harness 原生「外观」设置
- 非法/缺失配置一律回退内置默认（host 侧二次校验兜底，不信任客户端值）
- 首次安装（清空存储后加载）：兜底链**继承当前正在使用的模型**（含推理等级），其后补内置链去重

### 插件管理 tab

- 展示**当前会话**的动态 Cordis 插件清单（状态 / 版本选择 / 当前版本 / 审批 / 运行 / 停止 / 删除）
- 工具栏「**诊断日志**」：读取 host 侧环形缓冲（最近 300 行），展示每次优化的 `cfg / try / ok / fail` 摘要（含主模型、思考等级 effort、兜底链 chain、超时、模板来源），便于核对配置生效与故障排查

## 隐私

v1 纯文本优化：仅发送「模板 + 输入文本」到 LLM，**零上下文注入**（不带会话历史/文件/附件/画像）。

## 键盘可达性

- 按钮为原生 `<button>`：Tab 聚焦、Enter/Space 触发；`:focus-visible` 有可见焦点环
- 未启用全局快捷键（方案中 Ctrl+E 为可选，暂未启用；按钮本身键盘可达）

## 已知边界

- 撤回快照为内存态：刷新页面/插件重启后失效（与同类桌面实现一致）
- **页面刷新会卸载动态插件前端**（DSH 动态插件机制：client 半部附着于激活时的页面连接）：刷新后前端组件（优化按钮/设置页）消失属正常现象，在「模型与插件 → 插件管理 → 运行」或由 agent 重新运行插件（`cordis_run`）即可恢复；**配置本身持久化在 localStorage，刷新/重装不受影响**
- 取消后底层请求可能仍短暂运行（已通过 `iterator.return()` 尽力真取消）
- 发送消息后优化状态清空（按钮复位、无幽灵状态条）
- 优化质量（W8）：模板含 800 字符约束 + 语言匹配 + 防回显约束 + 中英双语示例；偶发质量不佳可通过再次优化迭代
- 插件管理页展示的是**当前会话**的插件：不同会话各有独立的插件空间（页面为空属正常，切到定义插件的会话即可看到）

