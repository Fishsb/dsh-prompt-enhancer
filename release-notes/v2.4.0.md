## ✨ 新功能

- **版本检测与一键更新**——插件管理页顶部新增检测卡片：检测任意公开 GitHub 仓库（默认本仓库）的版本状态，发现新版本可一键拉取 6 个发布文件到目标目录（默认 `<workspace>/dsh-prompt-enhancer-<tag>/`），并按页面指引应用（bundle 更新 / 动态重定义 / 脚本分发）
- **按钮三态字体锚点**——优化按钮空闲/执行中/撤回文字显式 13px/500/20px，与模型选择器对齐
