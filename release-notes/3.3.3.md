# v3.3.3 — 一键更新修复版

> 追加重发：修复一键更新 executor 崩溃（依赖缺失 + 端口链路），同步纳入批次A–D 与外部贡献处理。

## 🐛 修复

- **一键更新 executor 崩溃**：独立执行器补足 undici 依赖（自包含同步）；undici 缺失时降级直连、不再崩进程；executor.port 陈旧时无条件验证——端口被占自动落到动态端口
- **增强易断链修复**：看门狗 15s / 探测 12s / 失败缓存 5s，链耗尽整链重试一次——「慢但健康」模型不再被误杀
- **恢复被批次D 误删的 require 绑定**（asrDeploy/sys）+ package.json 描述 emoji 乱码修复

## 🛡️ 稳定性加固

- 更新器自动链熔断（显式代理 + 指数退避 + kill-switch + 重启防抖）
- 部署账本 + 自愈版本守卫 + 一致性自检
- probeEnv 全异步化（事件循环阻塞降至毫秒级）+ net 探测熔断冷却
- 日志全链 ISO 时间戳 + 发布物 BOM 收口

## ✨ 其他

- 外部 fork/PR 贡献评估与 README 采纳（badge 链接 + VAD 说明）
- package-lock 根版本同步（3.2.4→3.3.2）