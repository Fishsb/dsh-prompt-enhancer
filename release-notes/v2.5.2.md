## 🐛 修复

- **环境检测全部误报的根因**：服务进程的 PATH 缺 `system32`（v2.5.1 诊断实证）→ `sc`/`where`/`curl`/`cmd.exe` 不可达。修复：子进程 PATH 改为注册表读取（系统 HKLM + 用户 HKCU）合并，并强制兜底追加 `SystemRoot\System32`
- 连带修复：环境检测 service/account/pnpmInfo/net 四项恢复正常；重启链 `cmd.exe` 可达（不再 RESTART_SPAWN_FAILED）
- **pnpmInfo 文案错位**：pnpm 缺失时不再显示「服务不存在」文案，改独立文案「未找到 pnpm——请安装 pnpm」

## 🧪 测试

- 单测 47/47 通过
- 环境检测 7 项本机实测应全绿（网络✓ / 服务✓ / 账号✓ / 重启✓ / 端口✓ / 形态✓ / pnpm✓）
