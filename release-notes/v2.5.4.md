## 🐛 修复：一键更新重启稳定性

- **重启链缓冲 2s → 5s**：实测一键更新后新进程偶发读到安装命令（pnpm add）触发的 DSH 配置/包落盘中间态 → session-query-sqlite 崩溃 → AppExit 即停服务（需手动拉起）；5s 缓冲给落盘稳定时间，大幅降低触发概率
- **重启结果自检**：更新并交出重启链后，页面自动轮询服务恢复（最长 30s）——恢复后提示成功 + 刷新；若未恢复，明确提示「服务未自动恢复——请手动执行 net start dsh-web」，不再让用户自己排查

## 🧪 测试

- 单测 47/47 通过（U43 断言同步 5s 缓冲）
- 建议实测：一键更新完整链路（安装 → 重启 → 自检 → 刷新）
