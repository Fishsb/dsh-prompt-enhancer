## 🐛 修复

- **恢复 v2.4.4 槽位占位 id 修复**：`sidebar.footer.action` 占位 id 由 `cordis-panel` 改回插件唯一值 `cordis-panel-enh`
  - 背景：v2.4.5 曾无记录回退该修复（与基座 `dsh-client-ui-cordis` 同槽位同 id 冲突，update/重挂时 single-occupant duplicate / "Failed to load plugins"），v2.4.5–v2.4.7 的 GitHub 发布物均受影响
  - 本版恢复修复并重建 `lib/client.cjs`；本地 GUI 若已运行修复版，下次官方路径更新后与发布物对齐

## 🧪 测试

- 单测 43/43 通过（U40 prompts 一致性 + U41 texts 断言不变）
