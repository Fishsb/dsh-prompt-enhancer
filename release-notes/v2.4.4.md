## 🐛 缺陷修复

- **修复 `sidebar.footer.action` 占位注册冲突**——占位项 id 由 `cordis-panel` 改为插件唯一值 `cordis-panel-enh`，回避与基座 `dsh-client-ui-cordis` 的 `CordisPanel`（同槽位同 id）冲突，解决 update/重挂时的 single-occupant duplicate 与 "Failed to load plugins"（历史 v2.4.1-fix2 同源问题的再次根治）

## 🧹 内部

- 版本号同步：`PLUGIN_VERSION` 与 `package.json` 统一升至 `2.4.4`
