'use strict';
// Target module: version check / one-click update service.
// Current implementation lives in src/host/legacy/plugin-host.js and lib/updater-host.cjs.
function register(ctx) {
  // TODO(M2): provide update.service; M4 will add platform reload adapter.
}

module.exports = { register };
