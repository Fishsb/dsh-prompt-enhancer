'use strict';
// Target module: plugin inventory / run / stop RPCs.
// Current implementation lives in src/host/legacy/plugin-host.js.
function register(ctx) {
  // TODO(M2): provide plugin management service or delegate to official loader.
}

module.exports = { register };
