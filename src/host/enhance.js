'use strict';
// Target module: enhance engine / pipeline.
// Current implementation lives in src/host/legacy/plugin-host.js.
function register(ctx) {
  // TODO(M2): provide enhance.service and register pipeline handlers.
}

module.exports = { register };
